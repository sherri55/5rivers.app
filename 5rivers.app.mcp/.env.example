# 5rivers.app backend GraphQL endpoint (default: http://localhost:4001/graphql)
FIVERIVERS_GRAPHQL_URL=http://localhost:4001/graphql

# Optional: JWT token if your GraphQL API requires auth
# FIVERIVERS_AUTH_TOKEN=your-jwt-token
